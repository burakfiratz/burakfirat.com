---
// Pure-CSS disclosure (checkbox hack) — no JS needed for the menu itself.
interface NavItem {
	label: string;
	href: string;
}
interface Props {
	items: NavItem[];
}
const { items } = Astro.props as Props;
---

<div class="md:hidden">
	<input type="checkbox" id="mobile-nav-toggle" class="peer sr-only" />
	<label
		for="mobile-nav-toggle"
		class="inline-flex size-9 cursor-pointer items-center justify-center rounded-full text-text-muted hover:bg-bg-subtle hover:text-text"
		aria-label="Menüyü aç/kapat"
	>
		<svg class="size-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
			<path d="M4 6h16M4 12h16M4 18h16"></path>
		</svg>
	</label>

	<nav
		aria-label="Mobil gezinme"
		class="fixed inset-x-0 top-[57px] z-40 hidden flex-col gap-1 border-b border-border bg-bg px-6 py-4 peer-checked:flex"
	>
		{
			items.map((item) => (
				<a href={item.href} class="rounded-md px-2 py-2.5 text-body text-text hover:bg-bg-subtle">
					{item.label}
				</a>
			))
		}
	</nav>
</div>
