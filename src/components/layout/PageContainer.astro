---
interface Props {
	class?: string;
	wide?: boolean;
}
const { class: className = '', wide = false } = Astro.props;
---

<div class:list={['mx-auto w-full px-6', wide ? 'max-w-5xl' : 'max-w-3xl', className]}>
	<slot />
</div>
